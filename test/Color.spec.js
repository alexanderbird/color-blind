import { Color } from '../src/Color';

describe('Color', () => {
  describe('.fromHexString', () => {
    it('returns a Color object matching that hex code', () => {
      expect(true).toBe(true);
    });
  });
});
